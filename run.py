from dtp import app
from waitress import serve

if __name__ == "__main__":
    #serve(app)
    app.run(debug=True, host="0.0.0.0", port="5000")

#1009 lines of codes in .py files. 
#800 lines of codes in .css files.
#1281 lines of codes in .html files.
#3180 lines of codes in total.
